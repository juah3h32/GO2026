<div id="appRoot">
  <div id="planetContainer"></div>

  <div id="textOverlay">
    <div class="text-rotator fade" id="rotate"></div>
    <div class="text-main">LATINOAMERICA</div>
  </div>
</div>

<style>
html, body, #appRoot {
  margin: 0;
  padding: 0;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

/* PLANETA */
#planetContainer {
  position: fixed;
  inset: 0;
  z-index: 1;
  cursor: grab;
}

/* OVERLAY TEXTO (FANTASMA) */
#textOverlay {
  position: fixed;
  inset: 0;
  z-index: 5;

  pointer-events: none; /* 🔥 CLAVE */
}

/* TEXTO PRINCIPAL */
.text-main {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);

  font-size: clamp(48px, 10vw, 150px);
  font-weight: 900;
  text-transform: uppercase;
  color: white;
  text-shadow: 0 0 20px rgba(255,255,255,0.6);
  white-space: nowrap;

  pointer-events: none;
}

/* TEXTO ROTATOR (si lo usas) */
.text-rotator {
  position: absolute;
  top: 42%;
  left: 50%;
  transform: translateX(-50%);
  pointer-events: none;
}



</style>

