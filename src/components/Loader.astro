<div id="loader-wrapper" aria-hidden="true">
  <div class="loader-spinner"></div>
</div>

<style>
  #loader-wrapper {
    position: fixed;
    inset: 0;
    z-index: 99990;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #ffffff;
    transition: opacity 0.45s ease;
    pointer-events: none;
  }

  html.dark #loader-wrapper {
    background: #0f0f0f;
  }

  #loader-wrapper.loader-hidden {
    opacity: 0 !important;
    pointer-events: none !important;
  }

  .loader-spinner {
    width: 40px;
    height: 40px;
    border: 3px solid rgba(251, 103, 11, 0.2);
    border-top-color: #fb670b;
    border-radius: 50%;
    animation: loader-spin 0.7s linear infinite;
  }

  @keyframes loader-spin {
    to { transform: rotate(360deg); }
  }
</style>
